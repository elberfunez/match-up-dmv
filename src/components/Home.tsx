import React from 'react';
import Button from '@mui/material/Button';

const Home = () => {
    return(
        <div>
            <h1>Hello, MUI!</h1>
            <Button variant="contained" color="primary">
              Primary Button
            </Button>
        </div>
    );
}
export default Home;